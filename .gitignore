# Build-time only (Terser minifier) — NOT shipped to the browser.
# The built artifact game.bundle.js IS committed; its build tooling is not.
node_modules/

# OS / editor cruft
.DS_Store
*.swp

# Local scratch / preview contact sheets generated by asset scripts
assets/_*.png
assets/_*.json
